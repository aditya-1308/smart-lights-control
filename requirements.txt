# Core async HTTP client (WLED API communication)
aiohttp>=3.9.0

# Smart ceiling light (Tuya local control)
tinytuya>=1.15.0

# Global hotkeys (Tuya brightness & toggle shortcuts)
pynput>=1.7.6

# Virtual DS4 controller (PlayStation lightbar emulation)
# Requires ViGEmBus driver: https://github.com/nefarius/ViGEmBus/releases
vgamepad>=0.1.0

# Numerical operations (color array processing)
numpy>=1.26.0

# Environment variable loader (.env file parsing)
python-dotenv>=1.0.0

# Windows native APIs (shared memory, hotspot, process control)
pywin32>=306

# iRacing shared memory SDK (sim racing telemetry)
pyirsdk>=1.3.6

# HTTP requests (Razer Chroma bridge, CS2 GSI)
requests>=2.28.0
